/**
 * The ArrayHelper offers functionality to effectively traverse through arrays without
 * checking for holes etc.
 * @author Wolfgang Felbermeier
 */
class ArrayHelper {

	static getArrayIndexMap(){

	}

	/**
	 * Returns an array only consisting of elements which are contained in all
	 * supplied arrays (basically a logical AND operation on all arrays).
	 * The $fenrir id of the object needs to match the index of the object in the array.
	 * @param arr One or multiple arrays to apply the mask operation on.
	 */
	static maskArray(...arr) {
		arr.map((array) => {

		});
	}

}

module.exports = ArrayHelper;